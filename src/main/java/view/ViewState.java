package view;

/**
 * Enum representing the different states of the view in the Influencer Management System.
 */
public enum ViewState {
    INFLUENCER_LIST,
    USER_FAVORITES,
    EXPORT,
    IMPORT,
    USER_PROFILE,
    LOGIN,
    REGISTRATION
}

